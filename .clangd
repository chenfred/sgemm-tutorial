CompileFlags:
  CompilationDatabase: build
  Remove:
    # CMake/NVCC 会生成这些 CUDA 编译参数；clangd 只做代码分析，不认识 nvcc 专用参数。
    - -forward-unknown-to-host-compiler
    - --generate-code=*

---
If:
  PathMatch: '.*\.cu(h)?'
Completion:
  # CUDA 内建变量来自 Clang 私有隐式头，禁止补全时把该头显式插入源码。
  HeaderInsertion: Never
